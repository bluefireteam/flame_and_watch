import 'package:flutter/material.dart';

class Game extends StatelessWidget {
  @override
  Widget build(_) {
    return Expanded(
      child: Container(
        color: Colors.blue,
      ),
    );
  }
}
